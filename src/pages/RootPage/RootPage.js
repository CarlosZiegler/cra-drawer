import { ContentContainer, Image } from '../../components';
import MainImage from '../../assets/developer.gif';

export default function NotFoundPage() {
  return (
    <ContentContainer>
      <Image src={MainImage} />
    </ContentContainer>
  );
}
